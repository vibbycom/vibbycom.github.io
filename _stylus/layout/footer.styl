//
footer
    background-color $nav-bg
    font-size 12px
    span(1/1)

    .container
        padding 60px 15px
        .footer-logo
            +below($xs)
                display none
            svg
                width 130px
            .text,
            .shape
                fill #fff
        ul
            margin-left 0px
            padding-left 0px
            margin-bottom 0px
        ul li
            a
                color $link-color
                text-transform uppercase
                font-weight 500
                &:hover
                    color #fff

        .company
            span(1/4)
            +below($xs)
                span()

        .connect
            span(3/4)
            +below($xs)
                span()
            ul
                margin-top emRhythm(4, 1)
                +below($xs)
                    text-align center
                li
                    text-align center
                    display inline-block
                    +below($xs)
                        &:not(:last-child)
                            margin-right 20px
                    +above($xs)
                        float right
                        &:not(:last-child)
                            margin-left 30px 
            +below($xs)
                span()
            text-align right

        .legal
            span(3/4)
            +below($xs)
                span()
            ul
                li
                    text-align center
                    +above($xs)
                        float right
                        &:not(:last-child)
                            margin-right 20px
                .copyright
                    color $light-color
